# Supply-chain hardening: never run install-time scripts (preinstall/postinstall/
# binding.gyp) — the vector used by Shai-Hulud-style npm attacks. esbuild still works
# because its platform binary ships as an optionalDependency package, not a postinstall.
ignore-scripts=true

# exact versions only — no ^/~ drift
save-exact=true
