"""adbreak — all real logic for the ad-detector data + model pipeline.

CLI scripts in pipeline/scripts/ are thin wrappers around this package.
Colab notebooks `pip install -e` this package and call into it — never copy logic.
"""
