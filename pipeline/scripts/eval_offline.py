#!/usr/bin/env python3
"""Thin CLI: offline eval — model directly on held-out MP4s, per-window
predictions vs CSV labels, stratified per-subtype report. The primary, fast
eval loop (live phone-in-front-of-TV runs validate, never iterate).

Not yet implemented — built in plan/05-detection.md.
"""

raise SystemExit("not yet implemented — see plan/05-detection.md")
