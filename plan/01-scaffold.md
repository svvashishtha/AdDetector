# Plan 01 — Scaffold the repo
Seq: 1/5 · Prev: — · Next: 02-splitter.md
Design ref: README#repo-structure-locked, README#documentation--planning-model
Status: done · last done: step 1 · next: —

- [x] 1. Scaffold: git init; folder tree per README; root README (system design +
         canonical build order); CLAUDE.md; plan/ master + sub-plans;
         config/pipeline.yaml; gitignored data/ & models/; subfolder README stubs;
         pipeline package skeleton (config.py real, other modules stubbed).
      done when: git-init'd repo matches the README tree and docs are populated.
      reverse:   fresh repo; delete/redo.
